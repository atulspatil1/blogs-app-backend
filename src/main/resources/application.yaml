spring:
  application:
    name: blogs-app-backend

  datasource:
    url: jdbc:mysql://localhost:3306/personal_blog_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: atulspatil
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate :
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080


app:
  # JWT Configuration
  jwt:
    secret: 7a3f8c2e1b9d4f6a0e5c8b3d7f2a9e4c1b8d5f3a7e2c9b6d4f1a8e5c2b9d7f4a  # 256-bit hex key — change in production!
    expiration-ms: 86400000   # 24 hours

  # CORS - allow React dev server
  cors:
    allowed-origins: http://localhost:5173/